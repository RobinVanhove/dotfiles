# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!


# set $mod Mod1
set $alt Mod1
set $win Mod4

font pango:hack 10

# Use Mouse+$win to drag floating windows to their wanted position
floating_modifier $win

# start a terminal
bindsym $alt+Return exec konsole
bindsym $alt+f exec firefox

# kill focused window
bindsym $win+q kill

# start dmenu (a program launcher)
#bindsym $alt+d exec rofi -show run
#bindsym $alt+space exec i3-dmenu-desktop --dmenu='rofi -dmenu -i'
## start rofi pass
#bindsym $alt+t exec rofi-pass

# change focus
bindsym $win+h focus left
bindsym $win+j focus down
bindsym $win+k focus up
bindsym $win+l focus right
bindsym $win+Left focus left
bindsym $win+Down focus down
bindsym $win+Up focus up
bindsym $win+Right focus right

# move focused window
bindsym $win+Ctrl+h move left
bindsym $win+Ctrl+j move down
bindsym $win+Ctrl+k move up
bindsym $win+Ctrl+l move right
bindsym $win+Ctrl+Left move left
bindsym $win+Ctrl+Down move down
bindsym $win+Ctrl+Up move up
bindsym $win+Ctrl+Right move right

# resize window (you can also use the mouse for that)
bindsym $win+Shift+h resize shrink width 10 px or 10 ppt
bindsym $win+Shift+j resize grow height 10 px or 10 ppt
bindsym $win+Shift+k resize shrink height 10 px or 10 ppt
bindsym $win+Shift+l resize grow width 10 px or 10 ppt
bindsym $win+Shift+Left resize shrink width 10 px or 10 ppt
bindsym $win+Shift+Down resize grow height 10 px or 10 ppt
bindsym $win+Shift+Up resize shrink height 10 px or 10 ppt
bindsym $win+Shift+Right resize grow width 10 px or 10 ppt


# split in horizontal orientation
bindsym $alt+h split h

# split in vertical orientation
bindsym $alt+v split v

# enter fullscreen mode for the focused container
bindsym $win+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $alt+s layout stacking
bindsym $alt+w layout tabbed
#:bindsym $alt+e layout toggle split

# toggle tiling / floating
bindsym $alt+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $alt+space focus mode_toggle

# focus the parent container
bindsym $alt+a focus parent

# focus the child container
#bindsym $alt+d focus child

# switch to workspace
bindsym $alt+1 exec "i3-msg workspace 1; i3-msg scratchpad show"
bindsym $alt+2 exec "i3-msg workspace 2; i3-msg scratchpad show"
bindsym $alt+3 exec "i3-msg workspace 3; i3-msg scratchpad show"
bindsym $alt+4 exec "i3-msg workspace 4; i3-msg scratchpad show"
bindsym $alt+5 exec "i3-msg workspace 5; i3-msg scratchpad show"
bindsym $alt+6 exec "i3-msg workspace 6; i3-msg scratchpad show"
bindsym $alt+7 exec "i3-msg workspace 7; i3-msg scratchpad show"
bindsym $alt+8 exec "i3-msg workspace 8; i3-msg scratchpad show"
bindsym $alt+9 exec "i3-msg workspace 9; i3-msg scratchpad show"
bindsym $alt+0 exec "i3-msg workspace 10; i3-msg scratchpad show"
bindsym $alt+minus scratchpad show

# move focused container to workspace
bindsym $alt+Shift+1 move container to workspace 1
bindsym $alt+Shift+2 move container to workspace 2
bindsym $alt+Shift+3 move container to workspace 3
bindsym $alt+Shift+4 move container to workspace 4
bindsym $alt+Shift+5 move container to workspace 5
bindsym $alt+Shift+6 move container to workspace 6
bindsym $alt+Shift+7 move container to workspace 7
bindsym $alt+Shift+8 move container to workspace 8
bindsym $alt+Shift+9 move container to workspace 9
bindsym $alt+Shift+0 move container to workspace 10
bindsym $alt+Shift+minus move scratchpad

# bind [ and ] to prev, next workspace
bindcode $alt+34 workspace prev 
bindcode $alt+35 workspace next

# Autostart
exec --no-startup-id i3-msg 'workspace 8; exec thunderbird; workspace 1'
exec --no-startup-id i3-msg 'workspace 10; exec riot-desktop; workspace 1'
exec --no-startup-id i3-msg 'workspace 9; exec keepassxc; workspace 1'
exec --no-startup-id i3-msg 'redshift -l 50.8503:4.3517'
exec_always 'mpd'

bindsym $alt+Shift+s sticky toggle

# Use xprop to find class/instance
assign [class="Riot"] 10
assign [class="keepassxc"] 9
for_window [class="^Thunderbird$" instance="^Calendar$"] sticky enable border pixel 1
for_window [class="^Thunderbird$" instance="^Calendar$"] sticky enable border pixel 1
for_window [class="^Pinentry$" instance="^pinentry$"] floating enable border pixel 1
for_window [urgent="latest" class="^Thunderbird$"] focus
for_window [urgent="latest" class="^Firefox$"] focus
bindsym $alt+g [urgent="latest" class="Riot"] focus
for_window [urgent="latest"] focus
for_window [class="^Pavucontrol$"] floating enable border pixel 1
for_window [tiling] border pixel 1

# reload the configuration file
bindsym $alt+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $alt+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $alt+Shift+e exec i3-msg exit #"i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
	#position top
        #status_command i3status
        #status_command I3BCONTRIB=~/src/i3blocks-contrib i3blocks
#}

# polybar
#exec_always --no-startup-id "killall polybar; polybar main"

# Sound control
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 1 +5%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 1 -5%
bindsym XF86AudioMute exec pactl set-sink-mute 1 toggle

# Music control
bindsym XF86AudioPlay exec mpc toggle
bindsym XF86AudioPrev exec mpc prev
bindsym XF86AudioNext exec mpc next

# Brightness control
bindsym XF86MonBrightnessUp exec xbacklight -inc 5
bindsym XF86MonBrightnessDown exec xbacklight -dec 5

bindsym $win+p exec spectacle -a # take screenshot of current screen
bindsym $alt+p exec spectacle # take screenshot

# lock screen
#bindsym $alt+l exec $HOME/scripts/lock.sh
#exec xautolock -time 10 -locker $HOME/scripts/lock.sh

# Configure mousepad
exec synclient TapButton1=1 TapButton2=1 TapButton3=1

# xrandr
#exec_always xrandr --output VIRTUAL1 --off --output eDP1 --primary --mode 1920x1080 --pos 0x0 --rotate normal --output DP1 --off --output HDMI2 --off --output HDMI1 --off --output DP1-3 --mode 1920x1080 --pos 0x0 --rotate normal --output DP1-2 --off --output DP1-1 --off --output DP2 --off
# xmodmap
#exec xmodmap ~/.Xmodmap


## Plasma Integration
# Try to kill the wallpaper set by Plasma (it takes up the entire workspace and hides everythiing)
exec --no-startup-id wmctrl -c Plasma
for_window [title="Desktop — Plasma"] kill; floating enable; border none

## Avoid tiling popups, dropdown windows from plasma
# for the first time, manually resize them, i3 will remember the setting for floating windows
for_window [class="plasmashell"] floating enable;
for_window [class="Plasma"] floating enable; border none
for_window [title="plasma-desktop"] floating enable; border none
for_window [title="win7"] floating enable; border none
for_window [class="krunner"] floating enable; border none
for_window [class="Kmix"] floating enable; border none
for_window [class="Klipper"] floating enable; border none
for_window [class="Plasmoidviewer"] floating enable; border none
